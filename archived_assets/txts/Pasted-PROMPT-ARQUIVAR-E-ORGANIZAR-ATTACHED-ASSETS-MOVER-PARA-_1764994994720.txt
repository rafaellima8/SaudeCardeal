PROMPT: ARQUIVAR E ORGANIZAR ATTACHED_ASSETS (MOVER PARA archived_assets)

Objetivo:
Arquivar os assets antigos do diretório `attached_assets/` para liberar espaço sem perda irreversível. A ação será mover (não deletar) para `archived_assets/` e opcionalmente comprimir grandes buckets.

Regras:
1) Criar branch: cleanup/assets-archive-$(date +%Y%m%d-%H%M) e checkpoint inicial.
2) Listar todos os arquivos por tipo e tamanho, gerar `ASSETS_REPORT.md`:
   - imagens PNG: count + total size
   - PDFs: count + total size
   - TXTs: count + total size
   - ZIPs: count + size (identificar duplicados por hash)
3) Para cada categoria:
   - PNGs screenshots (ex.: 28 PNGs) -> sugerir mover 80% para `archived_assets/screenshots/` e manter 20% recentes/úteis. O Agent deve sugerir critérios (data, tamanho, última modificação).
   - PDFs manuais (20) -> mover TODOS para `docs/` (se forem documentações relevantes) OU `archived_assets/pdfs/` se não essenciais, dependendo de metadados.
   - TXTs (38) -> arquivar em `archived_assets/txts/` com índice `txt_index.csv` contendo origem e hash.
   - ZIPs duplicados -> manter 1 no `attached_assets/` + mover os demais para `archived_assets/zips/` ou `backups/`.
4) Procedimento de execução (aplicar automaticamente, mas com opção de revisão antes do commit):
   - Criar `archived_assets/` na raiz.
   - Para cada arquivo a mover: `git mv attached_assets/<file> archived_assets/<category>/<file>` (ou `mv` + `git add` se necessário).
   - Para grandes conjuntos (>10MB por item), comprimir em `archived_assets/archives/YYYYMMDD_assets.tar.gz` e manter um small pointer file com lista dos arquivos originais.
5) Pós-movimentação:
   - Rodar `pnpm -w -s lint`, `pnpm -w -s typecheck`, `pnpm -w -s test:unit` e `pnpm -w -s build`.
   - Criar `RELATO_ASSETS.md` com lista movida, hashes, e instruções de restauração (`git checkout <branch> -- archived_assets/...`).
6) NÃO deletar definitivamente — mover/arquivar apenas. Deletar só com APPROVAL explicito e PR.
Saída esperada:
- `ASSETS_REPORT.md`
- `RELATO_ASSETS.md`
- branch com `archived_assets/` commit e checkpoint
- pergunta ao proprietário: “Deseja deletar permanentemente os assets arquivados? (SIM/NAO)”
